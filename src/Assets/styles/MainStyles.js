import { primaryGreen, secondaryGreen } from '../styles/variables/variables';

const MainStyles = {
    top: {
        flex: 0,
        backgroundColor: primaryGreen,
    },
    container: { flex: 1, backgroundColor: secondaryGreen },
};

export default MainStyles;
