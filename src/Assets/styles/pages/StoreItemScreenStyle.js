import { StyleSheet } from 'react-native';

const StoreItemScreenStyle = StyleSheet.create({
    textContainer: {
        flex: 0.1,
        paddingLeft: 20,
        paddingTop: 20,
    },
});

export default StoreItemScreenStyle;
