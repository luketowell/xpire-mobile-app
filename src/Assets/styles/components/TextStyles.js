import { StyleSheet } from 'react-native';

const TextStyles = StyleSheet.create({
    small: {
        fontSize: 16,
    },
    medium: {
        fontSize: 22,
        textAlign: 'center',
    },
    large: {
        fontSize: 34,
        fontWeight: '700',
    },
});

export default TextStyles;
